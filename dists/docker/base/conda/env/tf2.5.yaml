# https://conda.io/projects/conda/en/latest/user-guide/tasks/manage-environments.html#create-env-file-manually
name: tf2.5
channels:
  - defaults
dependencies:
  - python=3.8
  - pip
  - pip:
      # common
      - ipykernel
      - ipywidgets
      - pydantic
      - requests
      - typer
      # - python-snappy==0.6.1
      # others
      - numpy
      - pandas
      # - plotly
      - protobuf==3.20.*
      # - scikit-learn
      # - scipy
      # - statsmodels
      - tensorboard==2.5.*
      # - tensorboard-plugin-profile
      # - nvidia-cudnn-cu11==8.9.6.50
      # - tensorrt-cu11
      # - tensorrt==8.6.1.post1
      - tensorflow==2.5.0 # tensorflow[and-cuda]
